import styled from 'styled-components'
import { cores } from '../../styles'

export const TituloCadastral = styled.h1`
  text-align: center;
  text-transform: uppercase;
  color: ${cores.corPrincipal};
`
